import 'package:flutter/material.dart';

class HomeBooksWidget extends StatelessWidget {
  const HomeBooksWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('books'),
    );
  }
}
